/**
 * Single default for `GEMINI_MODEL` when unset (Plan + nVibe ideation).
 * Prefer latest Flash from https://ai.google.dev/gemini-api/docs/models — override in `.env` if your key returns 404.
 */
export const DEFAULT_GEMINI_MODEL = "gemini-3-flash-preview";
