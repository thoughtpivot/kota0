# Host-side artifacts from the workspace's preview/Apply flow.
# The image must install fresh inside the builder stage.
node_modules
dist
.env

# Workspace cruft that may land in bundles/<appId>/ but doesn't belong in the image.
.git
.gitignore
*.log
.DS_Store
