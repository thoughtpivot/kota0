diff --git a/node_modules/koa-logger/index.js b/node_modules/koa-logger/index.js
index 26e6eaf..7a6f885 100644
--- a/node_modules/koa-logger/index.js
+++ b/node_modules/koa-logger/index.js
@@ -80,7 +80,8 @@ function dev (opts) {
     if (length == null && body && body.readable) {
       ctx.body = body
         .pipe(counter = Counter())
-        .on('error', ctx.onerror)
+        // Arrow preserves lexical `ctx`; passing `ctx.onerror` breaks `this` inside context.onerror (this.app undefined).
+        .on('error', err => ctx.onerror(err))
     }
 
     // log when the response is finished or closed,
